<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid d-flex justify-content-between">
        <div>
            <a class="navbar-brand h1" href="">@Brand</a>
            <button class="navbar-toggler" @onclick="ToggleNavMenu">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
            <ul class="navbar-nav align-content-center">
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="events">
                        <span class="oi oi-calendar" aria-hidden="true"></span> Събития
                    </NavLink>
                </li>
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="membership">
                        <span class="oi oi-plus" aria-hidden="true"></span> Членство
                    </NavLink>
                </li>
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="about">
                        <span class="oi oi-flag" aria-hidden="true"></span> За нас
                    </NavLink>
                </li>
                <li class="nav-item px-3">
                    <NavLink class="nav-link" href="contactUs">
                        <span class="oi oi-chat" aria-hidden="true"></span> Контакти
                    </NavLink>
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    private bool collapseNavMenu = true;

    [Parameter]
    public string Brand { get; set; }

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
